# Phase 0 exit review — 2026-09-02

Reviewer: independent agent pass over the full Phase 0 tree (the phase diff is the entire
repository at commit `9d4842e`), with live verification against the built addon and the
pinned core source (`v0.30.0` = `c8d0716`, confirmed against `Cargo.lock`). Review
contract: `AGENTS.md` invariants, plan 00 decisions D1–D9, plan 01 scope.

## Verdict

**PASS** after remediation — zero open REQUEST CHANGES findings.

## Findings and resolutions

| #   | Severity        | Finding                                                                                                                                                                                 | Resolution                                                                                                                                  |
| --- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | REQUEST CHANGES | `CHANGELOG.md` documented the pre-pivot error contract (`error.code`), contradicting the implemented `[LAMINAR_<n>]` message prefix (live-verified: `error.code === "GenericFailure"`). | Reworded to the message-prefix contract with the Phase 1 TS-layer pointer. Commit `b800718`.                                                |
| 2   | SUGGESTION      | Plan 00 §5 "never surface napi-level failures raw" is unachievable for napi-derive argument-coercion rejections (`execute(42)` rejects unprefixed; verified live).                      | Invariant scoped to engine failures; coercion failures are explicitly the TS wrapper's (D8) job.                                            |
| 3   | SUGGESTION      | Gate glosses in `AGENTS.md`/`README`/plan 01 said `cargo test` (bare) — which fails to link per the repo's own spike finding.                                                           | Glosses now match the justfile (`cargo test --features napi-noop`); plan 01 Task 0.4 wording corrected (`review` is a separate lint gate).  |
| 4   | SUGGESTION      | CI inlines the gate commands instead of installing `just`; plan text implied `just review`/`just verify` ran verbatim. Combined gate coverage verified complete.                        | Plan 01 Task 0.6 now describes the inlining. (`rust-cache` was already present on the review job — reviewer note was stale.)                |
| 5   | SUGGESTION      | The allows-grep pattern `#[allow(` missed `#[cfg_attr(..., allow(...))]`.                                                                                                               | Pattern tightened to `#\[(cfg_attr\([^)]*,\s*)?allow\(` in justfile and CI; the one existing cfg_attr allow now carries a same-line `WHY:`. |
| 6   | NOTE            | README quickstart mixed `import` and `require` and awaited sync `version()`.                                                                                                            | Snippet fixed.                                                                                                                              |

Verified-correct (no action): pin triple-agreement (`CORE_PIN.md` == `Cargo.toml` tag ==
`CORE_PIN_TAG` + unit test), edition/rust-version at the pin, async seam behavior
(idempotent close under triple-concurrent callers — exactly one shutdown;
`AcqRel`/`Acquire` ordering sound), no reachable `unwrap`/`expect`/panic on user data in
`src/`, generated surface agreement (`{open, Connection, version}`), 7-platform
`napi.targets` matching D9, no pnpm→cargo lifecycle scripts, smoke suite pins real
behavior with negligible flake risk, Conventional-Commits history with no AI attribution.

## Exit criteria status

- [x] `just verify` green locally (macOS arm64; 4 Rust + 7 vitest).
- [x] CI green on Linux + macOS from a clean checkout — run 33684956017 on `b800718`, all
      jobs, 14m14s; the pinned git-tag core resolves without sibling clones.
- [x] `just review` green; `AGENTS.md` committed; plan checkboxes updated.
- [x] This review record with zero open REQUEST CHANGES findings.
- [x] Conventional commit history; no AI/assistant attribution.

Phase 0 is closed; Phase 1 (plan 02) proceeds.
