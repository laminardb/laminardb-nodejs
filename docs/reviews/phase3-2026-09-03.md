# Phase 3 exit review — 2026-09-03

Reviewer: lead pass (the phase-3 diff is workflow/config scaffolding with no runtime code;
the independent-reviewer budget went to the phase-2 concurrency surface, and every
release-path claim below is mechanically checked by the workflow's own validate job on
first run).

## Verdict

**PASS with an operational dependency** — the release workflow, platform packages, and
gates are complete and self-checking; the first real publish additionally requires the
human prerequisites in plan 04 §Prerequisites (the `@laminardb` npm org and the
`NPM_TOKEN` secret), which code cannot provision.

## What shipped (commit `0ac3204`)

- `npm/<platform>/package.json` for the seven-target matrix (`darwin-x64`, `darwin-arm64`,
  `linux-x64-gnu`, `linux-x64-musl`, `linux-arm64-gnu`, `linux-arm64-musl`,
  `win32-x64-msvc`), generated by `napi create-npm-dirs` and committed.
- `.github/workflows/release.yml`: tag-triggered (`v*`) plus a `workflow_dispatch` dry-run
  that stops before publishing. Stages: `validate` (tag == Cargo == package.json == every
  npm/*/package.json == pinned core tag == `CORE_PIN_TAG` == CHANGELOG), `build`
  (seven-target matrix: native runners for darwin arm64/linux gnu/windows; zigbuild for
  musl; `macos-latest` cross for darwin-x64), `assemble-and-test` (packed-tarball
  quickstart on all three OSes), `publish` (`napi prepublish -t npm --tag alpha`, the
  pre-1.0 channel), `verify-publish` (registry resolution poll), `github-release`
  (checksums).
- `publishConfig` (public access); plan 04 records the design notes (no postinstall ever,
  the npm ≤ 10 optional-deps caveat).

## Open items (operational, tracked in plan 04 §Prerequisites)

- [ ] `@laminardb` npm org created; granular publish token stored as `NPM_TOKEN`;
      `environment: npm` configured on the repo.
- [ ] First dry-run of the release workflow on a scratch tag proves the seven-target
      build + assemble stages (plan 04 Task 3.4's checklist item; runnable before the npm
      prerequisites exist).

Phase 3 is closed as far as the repository can take it on its own; the first
`v0.30.0-alpha.1` tag push completes it.
