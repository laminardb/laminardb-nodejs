# Phase 1 exit review — 2026-09-02

Reviewer: independent agent pass over the Phase 1 diff (commits `76c75ac` + `0c26dbb`),
with live probes against the built addon and verification against the pinned core source
(`v0.30.0`). Review contract: `AGENTS.md` invariants, plan 00 D1–D9, plan 02 scope.

## Verdict

**PASS** after remediation — zero open REQUEST CHANGES findings.

## Findings and resolutions

| #   | Severity        | Finding (all probe-verified by the reviewer)                                                                                                                                                                 | Resolution                                                                                                                                                                                                                                                                                         |
| --- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | REQUEST CHANGES | BigInt inputs silently corrupted: `get_i64()`'s `lossless` flag discarded (2^63 wraps negative), and BigInt routed through f64 for int columns (2^53+1 loses).                                               | BigInt cells now carry exact i64 (lossless flag enforced, reject beyond 64-bit signed); int conversion never touches f64 for bigints. Regression tests round-trip 2^53+1 and ±i64::MAX and reject 2^63.                                                                                            |
| 2   | REQUEST CHANGES | `int_in_range` compared i64 bounds as f64: `i64::MAX as f64` rounds to 2^63, admitting it as a JS number and silently saturating on cast.                                                                    | Number inputs must be integral and within ±(2^53−1) — where f64 integers are exact — with a "pass a BigInt" message beyond; range checks run on the exact i64.                                                                                                                                     |
| 3   | REQUEST CHANGES | Date32/Date64 had no finiteness guard and truncated toward zero: NaN stored as 1970-01-01, ±Infinity saturated, pre-epoch values off by one day.                                                             | Non-finite and >2^53 magnitudes reject; temporal math is exact i64 with euclidean division (pre-1970 floors to −1); Date32 range-checked. Tests cover the floor and the rejections.                                                                                                                |
| 4   | REQUEST CHANGES | TS async wrappers invoked the native call as the _argument_ to `wrapAsync`, so napi argument-coercion throws escaped unwrapped (`conn.execute(42)` → raw `StringExpected`), contradicting plan 00 §5/README. | `wrapAsync` takes a thunk; every async call site (including `LaminarDB.open`) wraps both sync throws and rejections. Coercion tests added for sync and async paths.                                                                                                                                |
| 5   | SUGGESTION      | UInt64 read path clamped `u64 > i64::MAX` to i64::MAX silently.                                                                                                                                              | Now rejects with a 900-coded message pointing at `toIPC()` (unreachable at the pin via SQL, kept honest for engine output).                                                                                                                                                                        |
| 6   | SUGGESTION      | Plan Task 1.1 checked `deliveryGuarantee` and "validation errors" but the code had neither.                                                                                                                  | `deliveryGuarantee` explicitly deferred in the plan (with the reason); `maxNodeDataBytes` now validates finite/non-negative with a 100-coded error; plan updated in the same change.                                                                                                               |
| 7   | SUGGESTION      | `MAX_SAFE_MILLIS` guarded the unit-scaled value, making the bound unit-dependent.                                                                                                                            | Timestamp scaling is exact i64 `checked_mul` from an input already bounded at 2^53; overflow rejects with a message naming the unit's real span.                                                                                                                                                   |
| 8   | SUGGESTION      | `close()` latched closed before awaiting shutdown — a failed close never retried.                                                                                                                            | Failed shutdown un-latches so `close()` can be retried; semantics documented as converge-to-closed.                                                                                                                                                                                                |
| 9   | NOTE            | `insert`/`insertArrow`/`writer` are sync data-plane calls (AGENTS says only lock-free accessors are sync).                                                                                                   | Reviewer verified at the pin that both underlying paths are non-blocking (brief catalog read + `try_send`); plan 02 documents the divergence; on the ledger if catalog locking grows.                                                                                                              |
| 10  | NOTE            | `check_schema` compared only the first IPC batch; `writeArrow` parsed IPC before the 301 check.                                                                                                              | Every batch is schema-checked; the closed-writer check precedes parsing.                                                                                                                                                                                                                           |
| 11  | NOTE            | `QueryResult::collect` termination verified sound at the pin (recv_async loops past Lagged internally; Timeout unreachable).                                                                                 | No action; plan 02's 2048-batch headroom note confirmed accurate.                                                                                                                                                                                                                                  |
| 12  | NOTE            | Test-coverage gaps (would have caught 1–4).                                                                                                                                                                  | `__test__/remediation.spec.mjs` added: BigInt fidelity and rejection edges, temporal guards and Date32 floor, 302 schema mismatch, non-nullable null rejection, Date-object guidance error, unknown-source 200s, `toLaminarError` unit coverage, coercion wrapping on both sync and async methods. |
| 13  | NOTE            | Doc drift (README message example missing the row suffix; plan wording; `wrapSync`/`wrapAsync` exported publicly).                                                                                           | README fixed; plan corrected; the TS layer now exports only the error classes and `toLaminarError`.                                                                                                                                                                                                |

Verified-correct (no action): packaging (`main`/`types`/`files` consistent, nothing
sensitive packed), loader resolution matching the bare-quickstart staging, pin
triple-agreement, no production `unwrap`/`expect`/panic on user data, lifecycle and
error-prefix invariants.

## Exit criteria status

- [x] `just review` + `just verify` green (fmt, clippy `-D warnings`, machete,
      allows-grep, prettier, 9 Rust tests, 32 vitest tests).
- [x] Failing-path coverage per module with code assertions (remediation suite).
- [x] This review record with zero open REQUEST CHANGES findings.
- [x] Bare-quickstart passes from `npm pack` (cold consumer).

Phase 1 is closed pending CI green on the remediation commit; Phase 2 (subscriptions,
plan 03) proceeds after that.
