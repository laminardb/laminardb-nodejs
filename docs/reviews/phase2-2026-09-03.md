# Phase 2 exit review — 2026-09-03

Reviewer: independent agent pass over the Phase 2 diff (commit `b2b398a`), with live
probes against the built addon and verification against the pinned core source
(`v0.30.0`). Review contract: `AGENTS.md` invariants, plan 00 D1–D9, plan 03 scope.

## Verdict

**PASS** after remediation — zero open REQUEST CHANGES findings. Remediation landed in
commit `0ac3204` together with the Phase 3 release engineering.

## Findings and resolutions

| #   | Severity        | Finding (probe-verified by the reviewer)                                                                                                                                                                        | Resolution                                                                                                                                                                                                                                                                                                                                      |
| --- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | REQUEST CHANGES | `onClose` fired **twice** on terminal failure paths (`emit_error` plus the loop exit) — user cleanup ran twice.                                                                                                 | `onClose` is a one-shot delivered from exactly one place (the loop exit; the open-failure path delivers its own). Tests count invocations across all paths.                                                                                                                                                                                     |
| 2   | REQUEST CHANGES | The push-backpressure contract was false for async handlers: `call_async` with `Return = ()` resolves when the JS callback _returns_, not when its promise _settles_ (probe: 1.5 s handlers overlapped freely). | `DataCallback` carries `Return = Promise<()>` (napi's `Promise` implements `FromNapiValue` and `Future`); the reader awaits each delivery's settlement, rejecting handlers report once via `onError(900)` and stop delivery, and the TS facade normalizes sync handlers to always-returning promises. Regression test pins `maxInFlight === 1`. |
| 3   | REQUEST CHANGES | The plan claimed a weak-liveness test ("unreferenced subscription does not keep a process alive") that did not exist — flipping `Weak` to `false` would pass the whole suite.                                   | Child-process liveness probe added (`__test__/weak-liveness.spec.mjs`); a strong TSF ref fails it. Plan corrected.                                                                                                                                                                                                                              |
| 4   | SUGGESTION      | No test drove either terminal code (502 lag / 500 failure).                                                                                                                                                     | Terminal coverage added: pipeline stop ends pull with one `[LAMINAR_500]` rejection then end-of-stream; push delivers `onError(500)` + exactly one `onClose`.                                                                                                                                                                                   |
| 5   | SUGGESTION      | Generated `index.d.ts` referenced `DataCallback`/`ErrorCallback`/`CloseCallback` without declaring them (did not compile standalone).                                                                           | `ts_arg_type` attributes inline the callback signatures; the seam d.ts is self-contained.                                                                                                                                                                                                                                                       |
| 6   | SUGGESTION      | Seam doc drift: `onError(code, message)` two-arg wording vs the shipped object; bare-source rejection code (200, not 4xx/5xx); plan listed unshipped `Subscription.close()`/`subscribeFromEpoch`.               | All corrected; the plan's shipped surface matches the code.                                                                                                                                                                                                                                                                                     |
| 7   | NOTE            | After `cancel()`, queued frames drain before the `null` sentinel.                                                                                                                                               | Documented on `subscribe`/`nextFrame`.                                                                                                                                                                                                                                                                                                          |
| 8   | NOTE            | Concurrent `close()`: the second caller can resolve marginally before the reader stops.                                                                                                                         | Documented (first-caller-waits).                                                                                                                                                                                                                                                                                                                |
| 9   | NOTE            | Weak-ref GC mid-stream stops delivery silently with `isActive()` still true.                                                                                                                                    | Documented in plan 03 spike results.                                                                                                                                                                                                                                                                                                            |
| 10  | NOTE            | Lease wording overstated: the lease is budget accounting; extracted batches own their Arc-backed buffers.                                                                                                       | Plan corrected; eager conversion discipline unchanged.                                                                                                                                                                                                                                                                                          |

Verified-correct (no action): reader-task cancel paths deliver `Closed` to pending
`nextFrame`; `QueryHandle` genuinely owned by the `QueryStream` reader (core
drop-cancels); telemetry objects field-for-field faithful; `cancelQuery` conversion fails
closed; benchmark doc matches measured output; the 50-test count is real.

## Exit criteria status

- [x] All gates green (fmt, clippy `-D warnings`, machete, allows-grep, eslint, prettier;
      11 Rust + 50 vitest tests).
- [x] Failing-path coverage for 502/500/cancel/close (review finding 4).
- [x] Weak-liveness regression (finding 3).
- [x] CI green on the remediation commit (run on `0ac3204`).
- [x] This review record with zero open REQUEST CHANGES findings.

Phase 2 is closed.
